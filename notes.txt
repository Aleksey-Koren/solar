
TODO

    ** Create station monitor during creating station //todo need create

    Для админа:

    CRUD
    1. ModificationController with CRUD methods and 'EDIT_MODIFICATIONS' permission. Not for simple users.
    5. applyModification()    "api/inventory-item/{id}/modification" PATCH...
    6. Create ModificationTypeController and CRUD methods in it. With 'EDIT_MODIFICATIONS' permission. Not for simple users.
    7. Create ModificationPriceController with CRUD and findAll(Pageable, Filter filter).

    -----------------------------------------------------------------------------------
   (only entities created)         ++ 2. prices and price_products DDL
                                   ++ 3. refactor calculateSellPrice in inventoryShopFacade(), StarShipShopFacade()
                                   ++ 4. modification_types DDL  (id INT, title VARCHAR)
   (mappers and dtos not created)  ++ 5. Create ModificationPrice DDL, entities, service and repo.









