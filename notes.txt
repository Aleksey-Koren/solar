
TODO
    ++1. Endpoint to update UserRoom.lastSeenAt
    ++2. Endpoint for Put api/chat/room/{roomId}/title
            2.1 We need to send system message telling about changing chat title.
    ++3. For private rooms I should return "JSON style" title to front (example: "[\"1:вася\", \"5:петя\"]")


    ++ выставить корабль на аукцион (из ангара)

    ++ забрать купленный на аукционе корабль в ангар на станции

    ++ получить все корабли в ангаре пользователя

    ++ StationShopController.buyStarship()

    ++ StationShopController.sellStarship()

    ++ пересесть с корабля на корабль на станции


    ++ переложить всё (то что в инвентаре но нев слотах) из корабля в корабль в ангаре.

    ++ переложить одну вещь из инвентаря в инвентарь в ангаре.


    ++ добавить проверку в методе layCourse(): возможно ли двигаться с ускорением полученном в курсе, учитывая массу корабля?
       Если с фронта пришли данные превышающие, то в курс в базу записать максимально возможное значениею
                {метод подсчета массы уже есть. Пока что он выдает фикс. значение хардкодом}

    ++ сделать эндпоинт на получение timeFlowModifier property
                {нужен класс контроллер для получения пропертей фронтом. Пока что в нем сделать только один метод}

    -- рефакторинг: все уведомления об изменении денег нужно бросать в самом конце, максимально близко к респонсу, когда уже точно ничего не отвалится.


    ----------------------------------------Вопросы и ответы
    вопрос: организация системы объёма кораблей (добавление сокетов для контейнеров)
            ответ: да. контэйнеры втыкаются в слоты. Обьем корабля ограничивается количеством слотовю


    Вопрос:  обмен предметами между двумя пользователями
            ответ: нужно делать через бэкэнд. На фронте небезопасною Завтра прикинем схему таблицы. Будет не сложно.
