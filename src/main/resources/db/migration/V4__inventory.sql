create table inventory_type (
                                id int not null AUTO_INCREMENT,
                                title varchar(255),
                                primary key (id)
) CHARACTER SET utf8;
